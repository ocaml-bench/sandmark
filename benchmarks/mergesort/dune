(executable
 (name mergesort)
 (modules mergesort)
 (modes native)
 (libraries domainslib containers))

 (executable
  (name common)
  (modules common)
  (modes native)
  (libraries domainslib containers))

(alias 
  (name multibench_parallel)
  (deps mergesort.exe common.exe))
