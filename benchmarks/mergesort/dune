(executable
 (name mergesort)
 (modules mergesort))
(executable
  (name common)
  (modes native)
  (modules common))

(alias 
  (name buildbench)
  (deps common.exe))
(alias 
  (name multibench_parallel)
  (deps mergesort.exe))
